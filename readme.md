# Docker Class Mega Lab Instructions

1. Read the instructions in `lab-store-microservices.pdf`

2. The `solution` branch has the `Dockerfile` and `docker-compose.yml` files with the solution of the lab.